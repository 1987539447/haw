原生标签 #开头，自定义标签@开头
注释： <#-- -->
条件判断 <#if 表达式> <#else> </#if>
列表（序列） <#list seq as x> 循环输出 </#list>
插入，包含其它文件 <#include "xxx.html">
变量丢失： !给默认值 ${user!"不存在"}，??判断是否存在 <#if user??>,通过括号限定多级访问时父对象丢失的问题${(user.name)!"nobody"}